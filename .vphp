# Funkcja vphp do przełączania wersji PHP CLI
function vphp() {
  if [ -z "$1" ]; then
    echo "Usage: vphp <version>"
    return 1
  fi
  sudo update-alternatives --set php /usr/bin/php$1
  php -v
}